{
  "name": "worksphere",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "mongodb": "^7.5.0",
    "nodemailer": "^9.0.5"
  }
}
