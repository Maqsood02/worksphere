{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/send-task-email",
      "destination": "/api/send-task-email"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
